#include "main.h"

/**
 * print_alphabet - print alphabet lowercase
 *
 * Return: Always 0
 */

void print_alphabet(void)
{
	char i;

	for (i = 'a' <= 'z' i++);
	{
		_putchar(i);
	}

	putchar('\n');

}

